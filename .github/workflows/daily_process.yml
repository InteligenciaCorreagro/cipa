name: Proceso Completo Diario - Facturas y Notas Crédito

on:
  schedule:
    # Ejecutar todos los días a las 1:00 PM UTC = 8:00 AM hora Bogotá (UTC-5)
    - cron: '0 13 * * *'
  workflow_dispatch:  # Permite ejecución manual desde GitHub

jobs:
  procesar-facturas:
    runs-on: ubuntu-latest
    
    steps:
      # ============================================================
      # 1. PREPARACIÓN DEL ENTORNO
      # ============================================================
      - name: 📥 Descargar código del repositorio
        uses: actions/checkout@v4
      
      - name: 🐍 Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 Cache de dependencias pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: 🔧 Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 📁 Crear estructura de directorios
        run: |
          mkdir -p data output templates
          echo "✅ Directorios creados"
      
      # ============================================================
      # 2. RECUPERAR BASE DE DATOS DE EJECUCIÓN ANTERIOR
      # ============================================================
      - name: 💾 Descargar base de datos de ejecución anterior
        uses: actions/download-artifact@v4
        with:
          name: base-datos-notas-credito
          path: data/
        continue-on-error: true
        # continue-on-error permite que falle en la primera ejecución
      
      - name: 🔍 Verificar estado de la base de datos
        run: |
          if [ -f data/notas_credito.db ]; then
            echo "✅ Base de datos encontrada de ejecución anterior"
            echo "📊 Información de la BD:"
            ls -lh data/notas_credito.db
            
            # Mostrar estadísticas
            sqlite3 data/notas_credito.db "SELECT 'Notas pendientes: ' || COUNT(*) FROM notas_credito WHERE estado='PENDIENTE';"
            sqlite3 data/notas_credito.db "SELECT 'Notas aplicadas: ' || COUNT(*) FROM notas_credito WHERE estado='APLICADA';"
            sqlite3 data/notas_credito.db "SELECT 'Total aplicaciones: ' || COUNT(*) FROM aplicaciones_notas;"
          else
            echo "⚠️  Primera ejecución: Base de datos se creará automáticamente"
            echo "📝 Se inicializarán las tablas:"
            echo "   - notas_credito"
            echo "   - aplicaciones_notas"
            echo "   - facturas_rechazadas"
            echo "   - tipos_inventario_detectados"
          fi
      
      # ============================================================
      # 3. EJECUTAR PROCESO PRINCIPAL
      # ============================================================
      - name: 🚀 Ejecutar proceso de facturas y notas crédito
        env:
          # Credenciales API SIESA
          CONNI_KEY: ${{ secrets.CONNI_KEY }}
          CONNI_TOKEN: ${{ secrets.CONNI_TOKEN }}
          
          # Configuración Email
          EMAIL_USERNAME: ${{ secrets.EMAIL_USERNAME }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          DESTINATARIOS: ${{ secrets.DESTINATARIOS }}
          
          # Configuración Base de Datos
          DB_PATH: ./data/notas_credito.db
        run: |
          echo "=================================================="
          echo "🔄 INICIANDO PROCESO COMPLETO"
          echo "=================================================="
          echo "📅 Fecha: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "🕐 Zona horaria: America/Bogota"
          echo ""
          
          # Ejecutar proceso principal
          python main.py
          
          echo ""
          echo "=================================================="
          echo "✅ PROCESO COMPLETADO"
          echo "=================================================="
      
      # ============================================================
      # 4. GUARDAR RESULTADOS
      # ============================================================
      - name: 📊 Cargar Excel generado
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reporte-facturas-${{ github.run_number }}
          path: output/*.xlsx
          retention-days: 30
      
      - name: 📝 Cargar reportes de texto
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reportes-adicionales-${{ github.run_number }}
          path: output/*.txt
          retention-days: 30
      
      # ============================================================
      # 5. PERSISTIR BASE DE DATOS PARA PRÓXIMA EJECUCIÓN
      # ============================================================
      - name: 💾 Guardar base de datos actualizada
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: base-datos-notas-credito
          path: data/*.db
          retention-days: 90
          # La BD se mantiene 90 días para histórico
      
      # ============================================================
      # 6. MOSTRAR RESUMEN DE EJECUCIÓN
      # ============================================================
      - name: 📈 Mostrar resumen de ejecución
        if: always()
        run: |
          echo ""
          echo "=================================================="
          echo "📊 RESUMEN DE EJECUCIÓN"
          echo "=================================================="
          
          if [ -f data/notas_credito.db ]; then
            echo "💾 Estado de Base de Datos:"
            sqlite3 data/notas_credito.db "SELECT '  Notas pendientes: ' || COUNT(*) || ' (Saldo: $' || COALESCE(SUM(saldo_pendiente), 0) || ')' FROM notas_credito WHERE estado='PENDIENTE';"
            sqlite3 data/notas_credito.db "SELECT '  Notas aplicadas: ' || COUNT(*) FROM notas_credito WHERE estado='APLICADA';"
            sqlite3 data/notas_credito.db "SELECT '  Total aplicaciones histórico: ' || COUNT(*) FROM aplicaciones_notas;"
            sqlite3 data/notas_credito.db "SELECT '  Facturas rechazadas histórico: ' || COUNT(*) FROM facturas_rechazadas;"
            
            echo ""
            echo "📋 Aplicaciones de HOY:"
            sqlite3 data/notas_credito.db "SELECT '  ' || numero_nota || ' → ' || numero_factura || ' ($' || valor_aplicado || ')' FROM aplicaciones_notas WHERE DATE(fecha_aplicacion) = DATE('now') LIMIT 10;"
          fi
          
          echo ""
          echo "📁 Archivos generados:"
          if [ -d output ]; then
            ls -lh output/
          fi
          
          echo ""
          echo "=================================================="
          echo "✅ EJECUCIÓN FINALIZADA"
          echo "=================================================="
      
      # ============================================================
      # 7. NOTIFICACIÓN EN CASO DE ERROR (OPCIONAL)
      # ============================================================
      - name: 🚨 Notificar error (si falla)
        if: failure()
        run: |
          echo "❌ El proceso falló. Revisar logs arriba."
          echo "🔍 Puntos de verificación:"
          echo "  1. ¿Las credenciales están configuradas correctamente?"
          echo "  2. ¿La API de SIESA está disponible?"
          echo "  3. ¿El servidor SMTP está accesible?"
          echo ""
          echo "📧 Se recomienda revisar los secrets en:"
          echo "   Settings → Secrets and variables → Actions"